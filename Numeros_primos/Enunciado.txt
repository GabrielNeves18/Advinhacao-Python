Desenvolva um programa que, dado um número inteiro fornecido pelo usuário, verifique se o número 
é um número primo. O programa deve exibir uma mensagem indicando se o número é ou não primo, e 
também mostrar os divisores do número, se houver.